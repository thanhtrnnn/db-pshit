# Copy to .env and set your keys; NEVER commit your real key

# Primary Gemini key for SQL generation (falls back to GOOGLE_API_KEY when unset)
GOOGLE_SQL_API_KEY=your_sql_generation_key

# Dedicated key for taxonomy classification (falls back to GOOGLE_SQL_API_KEY when unset)
GOOGLE_TAXONOMY_API_KEY=your_taxonomy_key

# Legacy fallback for older scripts
# GOOGLE_API_KEY=your_sql_generation_key
